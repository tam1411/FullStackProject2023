{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "vite-node -w src/index.ts",
    "build": "vite build --emptyOutDir -c vite.config.ts -m production",
    "startProd": "http-server -p 8080 build/",
    "lint": "eslint . --ext .ts",
    "test": "vitest run --passWithNoTests",
    "test:ui": "vitest --ui",
    "test:watch": "vitest --passWithNoTests"
  },
  "scriptsComments": {
    "dev": "Starts the development server using vite-plugin-node for rapid iteration.",
    "build": "Builds the app for production.",
    "startProd": "Runs the built app in production mode. Note this requires globally-installed npm package http-server (pnpm i -g http-server).",
    "lint": "Lints the project.",
    "test": "Runs the test suite.",
    "test:ui": "Runs the test suite in the browser.",
    "test:once": "Runs the test suite once."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/middie": "^8.1.0",
    "@fastify/static": "^6.8.0",
    "cors": "^2.8.5",
    "fastify": "^4.11.0",
    "fastify-cors": "^6.1.0",
    "fastify-env": "^2.2.0",
    "fastify-plugin": "^4.5.0",
    "pino": "^8.8.0",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.12"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.60",
    "@swc/core": "^1.3.32",
    "@types/cors": "^2.8.13",
    "@types/node": "^18.11.18",
    "@types/supertest": "^2.0.12",
    "@typescript-eslint/eslint-plugin": "^5.50.0",
    "@typescript-eslint/parser": "^5.50.0",
    "@vitest/coverage-c8": "^0.28.3",
    "@vitest/ui": "^0.28.3",
    "c8": "^7.12.0",
    "eslint": "^8.31.0",
    "fastify-cli": "^5.7.0",
    "pino-pretty": "^9.1.1",
    "supertest": "^6.3.3",
    "typescript": "^4.9.4",
    "vite": "^4.0.4",
    "vite-node": "^0.28.3",
    "vite-plugin-node": "^3.0.2",
    "vitest": "^0.28.3"
  }
}
